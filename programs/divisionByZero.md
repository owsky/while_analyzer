# DivisionByZero

**Input state**: x: [0, 0], y: $⊤_{b}^{\text{\#}}$
**Input interval boundaries**: m = -∞, n = +∞
```pascal
begin
  y = 7 / x;
end
```
**Abstract loop invariants**:

**Output state**: $⊥^{\text{\#}}$
